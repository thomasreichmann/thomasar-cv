/** Static site metadata, shared between the document head and anywhere else
 * that needs the canonical name / description. */
export const site = {
  name: "thomasar-cv",
  description: "A résumé maintained as structured data.",
} as const;
