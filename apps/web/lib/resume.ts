/**
 * Default name a freshly created résumé carries until the user renames it.
 * Shared so the dashboard's "New résumé" and guest mode's first résumé start
 * identically; one definition keeps the two entry points from drifting apart.
 */
export const DEFAULT_RESUME_NAME = "Untitled résumé";
